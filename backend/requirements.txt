# FastAPI & ASGI
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.20

# Configuration
pydantic>=2.10.0
pydantic-settings>=2.7.0

# HTTP Client (for Ollama)
httpx>=0.28.0

# Vector DB
pinecone>=5.4.0

# Embeddings & Reranker
# FlagEmbedding requires torch — install torch first separately if needed
sentence-transformers>=3.3.0
torch>=2.6.0
transformers>=4.47.0

# LangGraph & LangChain
langgraph>=0.2.59
langchain>=0.3.13
langchain-core>=0.3.25
langchain-community>=0.3.13
langchain-text-splitters>=0.3.4
langsmith>=0.2.3

# PDF & Document Parsing
pypdf>=5.1.0
python-docx>=1.1.2

# Evaluation (optional — comment out if install fails)
# ragas>=0.2.6

# Logging
structlog>=24.4.0

# Utilities
aiofiles>=24.1.0
numpy>=1.26.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-mock>=3.14.0
